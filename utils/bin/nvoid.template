#!/bin/sh

export NIL_RUNTIME_DIR="${NIL_RUNTIME_DIR:-RUNTIME_DIR_VAR}"
export NIL_CONFIG_DIR="${NIL_CONFIG_DIR:-CONFIG_DIR_VAR}"
export NIL_CACHE_DIR="${NIL_CACHE_DIR:-CACHE_DIR_VAR}"

exec nvim -u "$NIL_RUNTIME_DIR/nil/init.lua" "$@"
